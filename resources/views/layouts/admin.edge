<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title ?? 'Admin' }} — Urban Tennis Club</title>
  <meta name="csrf-token" content="{{ csrfToken() }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/admin-dashboard.css">
  @stack('styles')
</head>
<body>

{{-- Toast notifications --}}
<div class="toast-wrap" id="toastWrap"></div>

{{-- Confirm dialog --}}
<div class="confirm-dialog" id="confirmDialog">
  <div class="confirm-box">
    <div class="confirm-icon" id="confirmIcon">⚠️</div>
    <div class="confirm-title" id="confirmTitle">Are you sure?</div>
    <div class="confirm-msg" id="confirmMsg">This action cannot be undone.</div>
    <div class="confirm-btns">
      <button class="btn btn-secondary" onclick="closeConfirm()">Cancel</button>
      <button class="btn btn-danger" id="confirmOk">Delete</button>
    </div>
  </div>
</div>

<div class="adm-app">

  {{-- ── SIDEBAR ──────────────────────────────────────────── --}}
  <aside class="adm-sidebar">

    <div class="adm-logo">
      <div class="adm-logo-icon">🎾</div>
      <div>
        <div class="adm-logo-name">Urban Tennis</div>
        <div class="adm-logo-sub">Admin Panel</div>
      </div>
    </div>

    <nav class="adm-nav">

      <div class="adm-nav-section">Main</div>

      <a href="{{ route('admin') }}" class="adm-nav-item {{ currentPage === 'dashboard' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/>
          <rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>
        </svg>
        Dashboard
      </a>

      <a href="{{ route('admin.bookings') }}" class="adm-nav-item {{ currentPage === 'bookings' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
          <line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/>
          <line x1="3" y1="10" x2="21" y2="10"/>
        </svg>
        Bookings
        @if(pendingCount && pendingCount > 0)
          <span class="adm-nav-badge">{{ pendingCount }}</span>
        @end
      </a>

      <div class="adm-nav-section">Management</div>

      <a href="{{ route('admin.courts') }}" class="adm-nav-item {{ currentPage === 'courts' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <path d="M3 3h18v18H3z"/><line x1="12" y1="3" x2="12" y2="21"/>
          <line x1="3" y1="12" x2="21" y2="12"/>
          <circle cx="12" cy="12" r="2"/>
        </svg>
        Courts
      </a>

      <a href="{{ route('admin.users') }}" class="adm-nav-item {{ currentPage === 'users' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
          <circle cx="9" cy="7" r="4"/>
          <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
        </svg>
        Users
      </a>

      <a href="{{ route('admin.payments') }}" class="adm-nav-item {{ currentPage === 'payments' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <rect x="1" y="4" width="22" height="16" rx="2" ry="2"/>
          <line x1="1" y1="10" x2="23" y2="10"/>
        </svg>
        Payments
      </a>

      <a href="{{ route('admin.slips') }}" class="adm-nav-item {{ currentPage === 'slips' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
          <polyline points="14 2 14 8 20 8"/>
          <line x1="9" y1="15" x2="15" y2="15"/>
          <line x1="9" y1="11" x2="15" y2="11"/>
        </svg>
        Slip Verify
        @if(pendingSlipsCount && pendingSlipsCount > 0)
          <span class="adm-nav-badge">{{ pendingSlipsCount }}</span>
        @end
      </a>

      <div class="adm-nav-section">Analytics</div>

      {{-- Reports group with sub-menu --}}
      @set('reportsOpen', currentPage === 'reports')
      <div id="navGroupReports">
        <div class="adm-nav-item {{ reportsOpen ? 'active' : '' }}"
             style="cursor:pointer;"
             onclick="toggleNavGroup('navGroupReports', 'reportsChevron')">
          <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
            <line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/>
            <line x1="6" y1="20" x2="6" y2="14"/>
            <line x1="2" y1="20" x2="22" y2="20"/>
          </svg>
          <span style="flex:1;">Reports</span>
          <svg id="reportsChevron" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"
               style="transition:transform .2s; flex-shrink:0; {{ reportsOpen ? 'transform:rotate(180deg)' : '' }}">
            <polyline points="6 9 12 15 18 9"/>
          </svg>
        </div>
        <div id="reportsMenu" style="{{ reportsOpen ? '' : 'display:none;' }} padding-left:12px;">
          <a href="{{ route('admin.reports') }}"
             class="adm-nav-item {{ reportSubPage === 'overview' ? 'active' : '' }}"
             style="font-size:13px; padding:6px 12px; gap:8px;">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
              <rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/>
              <rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>
            </svg>
            Overview
          </a>
          <a href="{{ route('admin.reports.coaches') }}"
             class="adm-nav-item {{ reportSubPage === 'coaches' ? 'active' : '' }}"
             style="font-size:13px; padding:6px 12px; gap:8px;">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
            Coach
          </a>
        </div>
      </div>

      <div class="adm-nav-section">System</div>

      <a href="{{ route('admin.settings') }}" class="adm-nav-item {{ currentPage === 'settings' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <circle cx="12" cy="12" r="3"/>
          <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/>
        </svg>
        Settings
      </a>

      <a href="{{ route('admin.profile') }}" class="adm-nav-item {{ currentPage === 'profile' ? 'active' : '' }}">
        <svg class="adm-nav-icon" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
          <circle cx="12" cy="7" r="4"/>
        </svg>
        My Profile
      </a>

    </nav>

    <div class="adm-sidebar-footer">
      <div class="adm-user-row">
        <div class="adm-user-avatar">{{ (auth.user.fullName ?? auth.user.email).charAt(0).toUpperCase() }}</div>
        <div>
          <div class="adm-user-name">{{ auth.user.fullName ?? auth.user.email }}</div>
          <div class="adm-user-role">Administrator</div>
        </div>
      </div>
      <form action="{{ route('admin.logout') }}" method="POST">
        {{ csrfField() }}
        <button type="submit" class="adm-logout-btn">
          <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
            <polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/>
          </svg>
          Sign Out
        </button>
      </form>
    </div>

  </aside>

  {{-- ── MAIN AREA ────────────────────────────────────────── --}}
  <div class="adm-main">

    {{-- Topbar --}}
    <header class="adm-topbar">
      <div class="adm-topbar-left">
        <div class="adm-breadcrumb">
          <a href="{{ route('admin') }}">Admin</a>
          @if(breadcrumb)
            <span class="adm-breadcrumb-sep">/</span>
            <span class="adm-breadcrumb-current">{{ breadcrumb }}</span>
          @end
        </div>
      </div>
      <div class="adm-topbar-right">
        <button class="adm-topbar-btn" title="Notifications">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
            <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
          </svg>
          @if(pendingCount && pendingCount > 0)
            <span class="adm-notif-dot"></span>
          @end
        </button>
        <a href="{{ route('admin.profile') }}" class="adm-topbar-avatar" title="Profile">
          {{ (auth.user.fullName ?? auth.user.email).charAt(0).toUpperCase() }}
        </a>
      </div>
    </header>

    {{-- Content --}}
    <main class="adm-content">
      @!section('content')
    </main>

  </div>
</div>

<script>
// ── Toast system ──────────────────────────────────────────
function showToast(msg, type = 'success') {
  const wrap = document.getElementById('toastWrap')
  const icons = { success: '✅', error: '❌', warning: '⚠️' }
  const t = document.createElement('div')
  t.className = `toast toast-${type}`
  t.innerHTML = `<span class="toast-icon">${icons[type]||'ℹ️'}</span><span class="toast-msg">${msg}</span><button class="toast-x" onclick="this.closest('.toast').remove()">×</button>`
  wrap.appendChild(t)
  setTimeout(() => { t.style.animation = 'toastIn .3s ease-out reverse'; setTimeout(() => t.remove(), 300) }, 4000)
}

// Flash messages from server
@if(flashMessages.has('success'))
  showToast({{ JSON.stringify(flashMessages.get('success')) }}, 'success')
@end
@if(flashMessages.has('error'))
  showToast({{ JSON.stringify(flashMessages.get('error')) }}, 'error')
@end
@if(flashMessages.has('warning'))
  showToast({{ JSON.stringify(flashMessages.get('warning')) }}, 'warning')
@end

// ── Confirm dialog ────────────────────────────────────────
let _confirmResolve = null
function showConfirm(title, msg, okLabel = 'Delete', icon = '⚠️') {
  return new Promise(resolve => {
    _confirmResolve = resolve
    document.getElementById('confirmIcon').textContent = icon
    document.getElementById('confirmTitle').textContent = title
    document.getElementById('confirmMsg').textContent = msg
    document.getElementById('confirmOk').textContent = okLabel
    document.getElementById('confirmDialog').classList.add('open')
  })
}
function closeConfirm() {
  document.getElementById('confirmDialog').classList.remove('open')
  if (_confirmResolve) { _confirmResolve(false); _confirmResolve = null }
}
document.getElementById('confirmOk').addEventListener('click', () => {
  document.getElementById('confirmDialog').classList.remove('open')
  if (_confirmResolve) { _confirmResolve(true); _confirmResolve = null }
})
document.getElementById('confirmDialog').addEventListener('click', e => {
  if (e.target === e.currentTarget) closeConfirm()
})

// ── Nav group toggle ──────────────────────────────────────
function toggleNavGroup(groupId, chevronId) {
  const menu    = document.getElementById(groupId).querySelector('[id$="Menu"]')
  const chevron = document.getElementById(chevronId)
  const isOpen  = menu.style.display !== 'none'
  menu.style.display    = isOpen ? 'none' : 'block'
  chevron.style.transform = isOpen ? '' : 'rotate(180deg)'
}

// ── Action menus ──────────────────────────────────────────
document.addEventListener('click', e => {
  document.querySelectorAll('.action-menu.open').forEach(m => {
    if (!m.contains(e.target)) m.classList.remove('open')
  })
  const toggle = e.target.closest('[data-menu]')
  if (toggle) {
    const menu = document.getElementById(toggle.dataset.menu)
    if (menu) {
      menu.classList.toggle('open')
      e.stopPropagation()
    }
  }
})

// ── CSRF helper for fetch ─────────────────────────────────
function csrfToken() {
  return document.querySelector('meta[name="csrf-token"]').content
}
async function apiPost(url, data = {}) {
  const res = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'X-CSRF-TOKEN': csrfToken() },
    body: JSON.stringify(data),
  })
  return res
}
</script>

@stack('scripts')
</body>
</html>
